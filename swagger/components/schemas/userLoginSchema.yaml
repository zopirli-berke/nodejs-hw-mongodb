type: object
properties:
  email:
    type: string
    format: email
    description: User's email address.
    example: 'user@example.com'
  password:
    type: string
    format: password
    description: User's password.
    minLength: 6
required:
  - email
  - password
