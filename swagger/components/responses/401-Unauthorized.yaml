description: Unauthorized access. Authentication is required.
content:
  application/json:
    schema:
      type: object
      properties:
        message:
          type: string
          example: 'Unauthorized access.'
